<html>
    <head></head>
    <body>
        <h1>{{contact.firstName}} {{contact.lastName}}</h1>
        <article>
            {{contact.message}}
        </article>
        <p>{{contact.phone}}</p>
    </body>
</html>
